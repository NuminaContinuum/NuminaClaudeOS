# Voice

> Created by `/setup`. Re-run `/setup` to update.

This file is a placeholder. Run `/setup` in Claude Code to fill it in. Every skill reads this file alongside `profile.md` to know how to *sound* when reflecting back to you.

The real `voice.md` will look something like:

```markdown
# Voice

**Last updated:** YYYY-MM-DD

## Stance
Derived from Q4 (inspirations). Sets the overall feel of how the companion speaks.

## Vocabulary
Reach for: terms drawn from the user's path that already feel native.
Avoid: terms that would feel imposed or foreign.

## Metaphor library
Three to five image families the user already lives inside.

## Question style
The shape of the deepening question. Open inquiry, pointing, image-attentive,
parts-aware, or body-first.

## Cadence
Spacious, crisp, gentle, lyrical, or declarative.

## Personality lens
Optional. If you gave a personality test in `/setup` (Enneagram, MBTI, Big Five, DISC),
a few directives on how suggestions get framed — e.g. an extravert profile biases toward
people-facing, expressive suggestions; an introvert profile toward solitary, low-stimulation
ones. Omitted entirely if you skipped it.

## Drift
After roughly twenty journal entries, blend with the user's own writing rhythm and
vocabulary. The voice profile is a seed, not a fixed setting.
```

The behavioural guidelines in `CLAUDE.md` (no diagnosis, no prescriptive advice, trauma-aware) always override the voice. A Jung-flavoured companion still does not project archetypes onto you. A Stoic-flavoured companion still holds space rather than instructs.
