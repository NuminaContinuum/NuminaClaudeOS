# Timeline

**Last updated:** —

A chronological log of milestones, challenges, insights, synchronicities, and initiations across your inner work.

Each entry has the format:
```
YYYY-MM-DD | type | one-line description | source-file
```

Types: `milestone` / `challenge` / `insight` / `synchronicity` / `initiation`

---

*No entries yet. Content-processing skills (`/journal`, `/dream`, `/integrate`, `/meditation`, `/harvest`, `/inner-review`) append entries here automatically when something notable surfaces. Run `/maps timeline` to refresh the formatting and grouping once entries accumulate.*
